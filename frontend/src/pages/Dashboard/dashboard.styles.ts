import styled from "styled-components";
import BaseContainer from "ui/basecontainer.component";

const DashboardContainer = styled(BaseContainer)`
  padding: 0;
  height: calc(100vh - 45px);
`;

export default DashboardContainer;
